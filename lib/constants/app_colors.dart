import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF2A579A); // Logo Blue
  static const Color primaryLight = Color(0xFF5C82CB);
  static const Color primaryDark = Color(0xFF00306C);
  static const Color accent = Color(0xFF808285); // Logo Grey
  static const Color background = Color(0xFFFFFFFF); // High contrast white for outdoor
  static const Color surface = Colors.white;
  static const Color error = Color(0xFFD32F2F);
  
  static const Color textPrimary = Color(0xFF000000); // Absolute black for outdoor visibility
  static const Color textSecondary = Color(0xFF424242); // Darker grey for better contrast
}
